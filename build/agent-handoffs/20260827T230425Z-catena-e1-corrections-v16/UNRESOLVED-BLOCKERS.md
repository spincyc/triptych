# Unresolved blockers — E1 Catena

This lane closed the semantic defects the V15 independent review required, and
the evidence defects that review named in the pipeline, and touched nothing
else. Everything below is recorded open and untouched, and none of it is
authorized by this lane's disposition.

| Blocker | Owner | State after V16 |
| --- | --- | --- |
| The V13 independent review is unpublished | The V13 reviewer | Open, and now historical. The V14 review is published at `0d11766ec232b2b4e46a7d1b0ada56ef22370004` and the V15 review at `67247ecc39a6e5f6224c64ca3ab1af163ee023b1` on `review/catena-wave-1-e1-corrections-v15-independent`, so this lane names the review it answers and the chain is intact from V14 forward. The V13 link stays empty; this lane does not fill it with a SHA it does not have. |
| Full sole-source semantic projection beyond the chapter | E1 continuation | Open. The chapter is projected once; the index, the bible roots, the paragraph layer and the absence sources are not. |
| Orphan raw sources | E1 continuation | Open. A source with no fragment standing under it still contributes an edition. |
| Source-only fragments counting | E1 continuation | Open. `{source: "0"}` still borrows its edition's author and work and is counted. |
| Scalar and nested translator coercion | E1 continuation | Open beyond the edition list an earlier lane normalized. Unchanged here. |
| Malformed and padded absence rows | E1 continuation | Open. |
| Broader selection and ordering defects | E1 continuation | Open. |
| Refusal verse typing | E1 continuation | Open. |
| Unreadable roots and the `src/web/data/bibles.json` prose | E1 continuation | Open. |
| Broader terminal and corrected-oracle proofs | E1 continuation | Open outside this lane's publication, payload and completion vectors. Inside them the body-application oracle is corrected and now fails at the exact parent; that correction is scoped to those vectors and proves nothing about the others. |
| CLI/web duplicated semantic model | Separate owner | Open. `scripts/_catena.py` and `src/web/browser/catena/catena-model.js` still state the corpus twice. |
| Model and combined route budget governance | Budget owner | Open, and materially larger. The model grew again, to 44,247 gzipped whole and 10,344 stripped, and carries no ceiling; this lane also relocated the page's remaining relocatable prose into it to stay under the page ceiling, and the page still ends larger than the parent left it, at 12,965/13,000, with 35 gzipped bytes of headroom. Two identified fixes costing 37 and about 60 gzipped bytes were deliberately deferred because they do not fit in 35 — disclosed and costed in `LIMITATIONS.md`, not paid for by a raised ceiling. No ceiling was raised and none is proposed here. |
| Historical data seam | Separate owner | Open. `src/web/data/` has zero changes in this lane. |
| `tools/mass-ordinary` example captures depend on build state | Examples and tool-registry owner | Open, and identified here rather than fixed here. `build/example-ordinary` is written by a later capture in the same target and is never cleaned up, and `mass-ordinary` appears in neither the `SCRATCH` nor the `PREPARE` table of `scripts/replay_examples.py`, so its two `check` captures diverge on a cold tree and match on a warm one — which is the whole of the gap the V15 review correctly noticed and misdiagnosed: 30 `divergent_rows` over 28 `distinct_divergent_identities` cold against 28 `divergent_rows` over 27 `distinct_divergent_identities` warm, one additional identity contributing two captured rows, with the warm figures historical and inferred from shipped V15 evidence rather than replayed here. `typeset-bible` already carries exactly the `PREPARE` entry that closes this shape, with a comment describing the identical problem. This lane derived the cause mechanically, disclosed it in `LIMITATIONS.md`, and changed neither the tool nor the replay script: both are outside a correction lane answering a review about publication atomicity. |
| Four stale release bindings | Release owner | Open, fail-closed and unsigned. None re-signed. `make -k check` fails closed on them, correctly. One of them is `src/web/browser/catena/index.html`, whose binding records `45c491ab…` while the file's actual SHA-256 is `7779d1f19ca175fd315cd7164f5347cc3c08d68b20b3b68a9219429b02bb8fa8`; where this package quotes that digest it is the digest OF THE FILE, and the binding for it is separately stale. |
| Common browser gate failures | Gate owner | Open. Identically red at both endpoints, reported as three separate figures — `total_gate_rows` 2,290, `normalized_reports_equal` yes, `distinct_diagnostic_names_or_categories` 17, the last of which is a diagnostic vocabulary and not the 2,290-row identity universe. Of those rows, 1,836 pass / 226 fail / 228 skip over 171 pages, 19 routes and 9 states on Chromium 151.0.7922.173 — with the 226 failing rows in exactly three names (`single-main-element` 117, `primary-controls-meet-target-size` 82, `skip-link-targets-existing-element` 27), entirely in inherited classes, and the two reports are equal under the named volatile exclusions, so nothing here moved. Nothing in this lane touches their causes. |
| B0 / shared shell | Shell owner | Open and untouched. |
| Real-device and assistive-technology evidence | Separate owner | Open. Not produced by this lane, and the vectors this lane closes are not reachable by a browser engine at all. |
| Protected Liturgy and PDFs | Protected owners | Untouched. |
| Packaged parent-only PDF error | Separate owner | Open and unrelated. `test_pdf_review.PdfReviewCommandTests.test_repeated_signals_do_not_interrupt_child_cleanup` was established as a signal-timing flake two reviews ago. This lane does not fix PDF and claims nothing about it. |
| Broader retained-artifact privacy coverage | Pipeline owner | Open. The published archive and its named siblings are scanned and re-scanned; discard and supersession markers, retired ledgers, the lane-wide executed-tool journal and retained discarded package trees are not, and no broader all-retained-artifacts claim is made. The V15 review declined that claim and this lane does not renew it. |
| E1 integration | Master-plan owner | Not integrated. `origin/main` stands at `2778285849f2973ea89d1cfd5b2751ed4ae58e54`; this lane is not merged into it, and no merge, re-sign, deploy or cutover was attempted. |
