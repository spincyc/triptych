# Unresolved blockers — open on purpose, none touched here

The V7 independent review enumerated its findings in the durable
`PROJECT-WORK.md` record on its review branch; the V8 and V9 independent
reviews reconfirmed every one of them open while adding, and then closing
through the V9 and V10 lanes, the composed escape, the package final-byte
defect, and the presentation and protocol findings this lane answers. Those
review records remain the authoritative statement of each. This lane
executed the V9 review's **exact next action** — the refused-prefix
presentation, the exact terminal vectors, and the protocol-correct package
— and deliberately touched nothing else. Everything below is open, listed
in the V7 review's own order, with its owner.

## Required production corrections (the V7 review's numbering)

1. **Projection is not yet the sole semantic source.** `chapterVoices()`
   and `absenceRows()` rescan raw sources; an orphan source manufactures a
   voice offer and an absence row; the generator iterates a scalar
   `translators` into letters. Owner: next authorized E1 correction lane.
2. **The text namespace at the request sink** — V8's isolated closure and
   V9's composed closure, both held; the presentation of the refused state
   is this lane's (`PRESENTATION-CLOSURE.md`).
3. **A `source`-only fragment is invalid and still counts.** The blessing
   oracle still stands beside the source/locator fixture. Owner: next E1
   lane; the producer contract it must enforce is E0's.
4. **Absence members still manufacture rows and suppress valid siblings.**
   Including the padded `" none-published "` false negative and the
   generator's coercion path. Owner: next E1 lane.
5. **Partial selection is neither closed nor order-independent.** Including
   the non-injective NUL-delimited ranking key. The prefix composition and
   presentation this and the prior lane closed are not the general case.
   Owner: next E1 lane.
6. **A refusal is not a whole typed refusal member.** The kind-dependent
   `verse` is unchecked in five shapes. Owner: next E1 lane.
7. **Unreadable roots still become Catena claims.** Trimmed data
   identities, incomplete `spineUnreadable`, empty-layer over-claims,
   break-at-missing verse, and the shared `verses` negative. Owner: next E1
   lane.
8. **An unreadable `src/web/data/bibles.json` is locally suppressible.**
   The review reversed the V7 lane's shared-shell reading: the forwarding
   seam is route-owned and must carry neutral copy. Owner: next E1 lane.
9. **Terminal proof remains partial outside this lane's vectors.** The full
   late-work sink vector for every corrected class; the V9 lane proved the
   composed prefix/fallback branch and this lane pinned its four vectors to
   expected values — the remaining corrected classes' vectors are open.
   Owner: next E1 lane.
10. **Several corrected oracles still bless the defects.** Owner: next E1
    lane, alongside each correction above.

## Architecture and payload

- **Duplicated semantic model** across `scripts/_catena.py` and the browser
  model; one neutral typed model callable by both surfaces is a separately
  authorized integration lane, per the review.
- **Uncapped combined route payload** — re-asked in `REVIEW_REQUEST.md` §6;
  the decision is the governing owner's. This lane grew the unbudgeted
  model again and discloses it.

## Package and evidence findings from the V9 review

The seven protocol findings — overwritten logs, after-the-fact provenance,
an unbound P8 transcript, missing duplicate-row and final-byte reporting,
stale labels and a collapsed decomposition, a deleting assembler, and the
missing handoff/review-request structure — are answered **for this
package** by the corrected pipeline in `logs/` and its products. The V7–V9
packages themselves are named, untouched, on the evidence branch, and
remain as their reviews left them.

## Separately owned, not this lane's and not the next E1 lane's

The historical `src/web/data/` Day-reader guard contradiction; the four
stale release bindings, fail-closed and unsigned; the common browser gate's
inherited failure population; B0/shared-shell; real-device and
assistive-technology evidence; protected Liturgy; PDF prerequisites; and
the eventual reconciliation of the cumulative implementation and review
history at integration time.
