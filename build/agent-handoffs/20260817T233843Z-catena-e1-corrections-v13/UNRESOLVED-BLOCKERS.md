# Unresolved blockers — open on purpose, none touched here

The independent reviews of this correction family enumerated their findings in
the durable `PROJECT-WORK.md` records on their own review branches, and those
review records remain the authoritative statement of each. This lane executed
the V12 review's **exact next action** — normalize each loaded chapter's
request-critical spine and fragment state once, retain that trusted
projection, pass it unchanged through readability, tally, request, cache, body
and ownership, add walking tests that assert one total observation per loaded
record, make all six planted parent scenarios non-vacuous and ship their
ownership journals, and issue a fresh immutable package with one complete
append-only lane ledger, no ordinal reuse, truthful terminal reasons and
times, post-P8 authority bound to the archive, a substantive inventory,
executed and shipped digests for every claimed tool, and privacy scanning of
every member and outer artifact — and deliberately touched nothing else.

"Owner" below names the lane or role authorized to do the work. It is not a
person, and no individual is named anywhere in this package.

Everything below is open.

## Required production corrections

1. **Projection is not yet the sole semantic source beyond this bounded
   chapter projection.** One chapter's request-critical state is now
   normalized once and held, and every chapter consumer answers from it. That
   is not the same as making the projection the sole semantic source for the
   route: the wide fragment and edition contracts are still validated field by
   field, and the projection was deliberately not broadened into all Catena
   semantics.
   Owner: the next authorized E1 correction lane.
2. **Orphan raw sources still manufacture offers and rows.** An orphan source
   manufactures a voice offer and an absence row that no projected record
   entitles. Gathering the offers and the editions in one walk did not change
   which sources are entitled to be there.
   Owner: the next authorized E1 correction lane.
3. **A `source`-only fragment is invalid and still counts.** The blessing
   oracle still stands beside the source/locator fixture.
   Owner: the next authorized E1 correction lane.
4. **Scalar and nested translator coercion.** The generator iterates a scalar
   `translators` value into letters, and the nested case is unhandled.
   Owner: the next authorized E1 correction lane.
5. **Malformed and padded absence findings.** Absence members still
   manufacture rows and suppress valid siblings, including the padded
   `" none-published "` false negative and the generator's coercion path.
   Owner: the next authorized E1 correction lane.
6. **A refusal is not a whole typed refusal member.** The chapter's recorded
   refusals are now normalized once into frozen `{kind, chapter, note}` rows,
   which fixes when they are read and not what a refusal must contain; the
   kind-dependent `verse` is still unchecked in five shapes.
   Owner: the next authorized E1 correction lane.
7. **Broader partial selection is neither closed nor order-independent.**
   Including the non-injective NUL-delimited ranking key. The prefix
   composition, presentation and claim closure this family of lanes closed are
   not the general case.
   Owner: the next authorized E1 correction lane.
8. **Unreadable roots still become Catena claims.** Trimmed data identities,
   the incomplete readability question, empty-layer over-claims,
   break-at-missing verse, and the shared `verses` negative. Readability is
   now decided from the same single walk that builds the rows, which changes
   when it is decided and not what it asks.
   Owner: the next authorized E1 correction lane.
9. **An unreadable `src/web/data/bibles.json` is locally suppressible, and its
   prose is unreviewed.** The V7-era reading that this was shared-shell
   behaviour was reversed by review: the forwarding seam is route-owned and
   must carry neutral copy.
   Owner: the next authorized E1 correction lane.
10. **Broader terminal proof remains partial outside this lane's vectors.**
    The full late-work sink vector for every corrected class is open. This
    lane re-ran the genuinely-late vector at all 36 guarded fields as a
    regression and added six walking request, cache, body and ownership
    vectors for the chapter's own members; the remaining corrected classes
    have no such vectors.
    Owner: the next authorized E1 correction lane.
11. **Several corrected oracles still bless the defects above.** This finding
    is recorded by the reviews as a class and **is not enumerated** — neither
    by the review records nor by this package. That is a gap in this record,
    stated rather than hidden, and it is not a claim that the class is small.
    Enumerating it is itself work that has not been done.
    Owner: the next authorized E1 correction lane, alongside each correction
    above.

## Architecture and payload

- **The CLI and web surfaces carry duplicated semantic models.**
  `scripts/_catena.py` and the browser model implement the same semantics
  twice. One neutral typed model callable by both surfaces is a separately
  authorized integration lane, per review.
  Owner: a separately authorized integration lane.
- **The uncapped model and combined route payload are disclosed, not
  governed.** This lane grew the unbudgeted model from 34,367 to 36,679
  gzipped whole and 8,258 to 8,873 stripped, and discloses it; it is the third
  consecutive lane to put a correction where the ceiling is not. Whether the
  model and the combined route payload need a governed ceiling is unanswered;
  `REVIEW_REQUEST.md` re-asks it.
  Owner: the budget owner.
- **The historical data-seam contradiction.** The historical `src/web/data/`
  Day-reader guard contradiction is unresolved.
  Owner: separately owned; not this lane and not the next E1 lane.

## Separately owned, and none of them this lane's

- **Four stale release bindings**, fail-closed and unsigned. None was
  re-signed here, and `make check-release-bindings` is red at both endpoints
  because of them.
- **The common browser gate's inherited failure population** — two `<main>`
  elements introduced by the release layout wrapping, and undersized
  navigation targets at the handset width. They are present on every route
  captured, including routes this correction does not touch.
- **B0 and the shared shell.**
- **Real-device and assistive-technology evidence.** None exists for this
  route; headless Chromium is not a device and is not an assistive technology.
- **Protected Liturgy.**
- **PDF prerequisites.**
- **The eventual reconciliation** of the cumulative implementation and review
  history at integration time.

## Package and evidence findings

The V12 review's package findings — authority established before verification,
the six contradictions its gate accepted, the ledger that was not append-only
in practice, the lexical inventory check, the two privacy leaks, and the
executed-byte claim that covered four tools of fifteen — are answered **for
this package** by the corrected pipeline shipped under `logs/` and by its
products. `PROVENANCE.md` states the phase ordering, the ledger rules and what
each record is authority for; the complete lane ledger and the final authority
record ship beside the package and are named in `HANDOFF.md` §10.

One item that this list carried from the previous lane is **closed here**: the
page no longer projects one chapter spine three times per render. The V12
review placed that finding inside the stable-snapshot correction rather than
deferring it as a broader blocker, and it is answered rather than listed.

Both V11 packages and the V12 package remain untouched on their evidence
branches, exactly as their reviews left them; none is mutated and none is
re-sealed here.

**E1 is not integrated.** Current `origin/main` is
`549bf0790503bd873dd8ce6ea0a64cc34f91271d` and this lane is not merged with
it. No merge, re-signing, deployment or cutover is authorized by anything in
this package.
