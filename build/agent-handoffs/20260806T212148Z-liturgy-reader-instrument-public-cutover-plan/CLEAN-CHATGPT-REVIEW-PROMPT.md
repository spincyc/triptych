# Clean independent review prompt

Review the immutable Liturgical Instrument public-cutover planning package in
this directory. Start with `START-HERE.md` and answer every question in
`REVIEW_REQUEST.md` with an explicit disposition and exact blocker if any.

This is planning only. Do not infer cutover authorization. Evaluate whether the
same-path mechanism, state compatibility classifications, proposed patch
boundary, static-hosting/indexing/cache treatment, ordinary-revert rollback,
and pre/post-deployment gates are sufficient for a later clean execution agent.
Pay particular attention to the enumerated Day default/deferred-state, Propers
public-key, candidate metadata/wording, and cross-entrance navigation choices.

The accepted visual foundation and production integration are frozen. Public
navigation and public cutover execution remain unauthorized.
