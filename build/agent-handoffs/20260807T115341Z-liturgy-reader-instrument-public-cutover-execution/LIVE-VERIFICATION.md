# Immediate live verification

The cache-bypassed pass completed at `2026-08-07T12:16:10.158Z` using a fresh
Chrome profile, disabled browser cache, no-cache request headers, and reloads
that ignored cache.

Result: PASS, 936/936 assertions across 36 original-pixel screenshots. It covers
canonical Day Read/Missal at governing desktop/intermediate/mobile widths, deep
scroll, Roman partial, postconciliar partial-English, why=1, two real territorial
branches, all four surfaces, 200% text, forced colors, keyboard focus, reduced
motion, Propers Read/Browse, stable cycle and translation-witness, unsupported
alternative fail-closed, and direct/reload/Back/Forward lifecycles.

Observed global problems: zero console, failed-request, HTTP, unnamed-control,
duplicate-ID, or required-overflow failures. Canonical pages are indexable;
candidates and oracle are statically full-noindex and publish no Open Graph
public URL. All 15 checked deployed HTML/JS/CSS assets byte-match the locked
public-alpha build. Details links point directly between canonical readers and
retain the route-owned contextual destinations.

Machine result: `evidence/live/immediate/browser-results.json`.
