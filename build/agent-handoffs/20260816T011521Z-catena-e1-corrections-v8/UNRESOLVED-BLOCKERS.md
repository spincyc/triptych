# Unresolved blockers — open on purpose, none touched here

The V7 independent review enumerated its findings in the durable
`PROJECT-WORK.md` record on its review branch; that record remains the
authoritative statement of each. This lane executed the review's **one exact
next action** — the namespace closure, item 2 of its required production
corrections — and deliberately touched nothing else. Everything below is
open, listed in the review's own order, with its owner.

## Required production corrections (the review's numbering)

1. **Projection is not yet the sole semantic source.** `chapterVoices()` and
   `absenceRows()` rescan raw sources; an orphan source manufactures a voice
   offer and an absence row; the generator iterates a scalar `translators`
   into letters. Owner: next authorized E1 correction lane.
2. **The text namespace at the request sink** — closed by this lane;
   `NAMESPACE-CLOSURE.md` is the record.
3. **A `source`-only fragment is invalid and still counts.** The blessing
   oracle still stands beside the source/locator fixture. Owner: next E1
   lane; the producer contract it must enforce is E0's.
4. **Absence members still manufacture rows and suppress valid siblings.**
   Including the padded `" none-published "` false negative and the
   generator's coercion path. Owner: next E1 lane.
5. **Partial selection is neither closed nor order-independent.** Including
   the non-injective NUL-delimited ranking key. Owner: next E1 lane.
6. **A refusal is not a whole typed refusal member.** The kind-dependent
   `verse` is unchecked in five shapes. Owner: next E1 lane.
7. **Unreadable roots still become Catena claims.** Trimmed data identities,
   incomplete `spineUnreadable`, empty-layer over-claims, break-at-missing
   verse, and the shared `verses` negative. Owner: next E1 lane.
8. **An unreadable `src/web/data/bibles.json` is locally suppressible.** The review reversed
   the V7 lane's shared-shell reading: the forwarding seam is route-owned and
   must carry neutral copy. Owner: next E1 lane.
9. **Terminal proof remains partial.** The full late-work sink vector and the
   parameterized production replay for every corrected class. Owner: next E1
   lane.
10. **Several corrected oracles still bless the defects.** Owner: next E1
    lane, alongside each correction above.

## Architecture and payload

- **Duplicated semantic model** across `scripts/_catena.py` and the browser
  model; one neutral typed model callable by both surfaces is a separately
  authorized integration lane, per the review.
- **Uncapped combined route payload** — re-asked in `REVIEW_REQUEST.md` §6;
  the decision is the governing owner's.

## Package and evidence corrections from the V7 review

The truthfulness and supersession requirements the review stated for
packages are answered **for this package** by its own derived claims and
declared-supersession statement (it supersedes nothing); the V7 packages
themselves are named, untouched, on the evidence branch, and remain as the
review left them.

## Separately owned, not V8's and not the next E1 lane's

The historical `src/web/data/` Day-reader guard contradiction; the four
stale release bindings, fail-closed and unsigned; the common browser gate's
inherited failure population; B0/shared-shell; real-device and
assistive-technology evidence; protected Liturgy; PDF prerequisites; and the
eventual reconciliation of the cumulative implementation and review history
at integration time.
