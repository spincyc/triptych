# Liturgical Instrument public-cutover compatibility handoff

## Disposition

Candidate for narrow independent compatibility review. Compatibility product
implementation is commit `3f3949617a04ffa68a1070058d0f7bc5ac74dc93`.
Evidence hardening is commit
`998648c341691c0807b0c209f93fbae16d641d48`. GitHub Pages runs
`31148986910` and `31150296458` succeeded for those exact commits.

No canonical public route or repository-owned public-navigation byte changed.
Public cutover execution remains unauthorized.

## Compatibility outcomes

- `why=1` renders existing production-derived reasoning as subordinate native
  apparatus after the affected branch, retains exact loci and held Latin, and
  has no recursive/self route link.
- Every held territorial branch renders under its existing source label and
  branch-specific result. No geography, array-order winner, or public locality
  key is inferred.
- `cycle`, `alternative`, and `translation-witness` are the stable Propers
  names. Retained `_candidate-*` spellings are input-only and serialization
  emits only stable names. Unsupported explicit alternatives remain explicit
  and fail closed.
- Retained candidates have the full static
  `noindex, nofollow, noarchive, nosnippet, noimageindex` directive; visible
  controller copy is route-neutral.
- Details presents the counterpart first, then restrained existing contextual
  destinations, without adding a fifth primary action.

## Evidence

The 13 original-pixel screenshots in `evidence/screenshots/` cover ordinary
Why desktop/mobile, both territorial branches desktop/mobile, territorial Why,
the rare no-seat Latin case, postconciliar Missal Why, and Day/Propers Details
desktop/mobile. `13-day-territorial-second-branch-393x852.png` is intentionally
scrolled to the second branch.

Browser results record Day 40/40, Propers 32/32, shared shell 18/18, and visual
24/24 over 113 captures. No governed console, failed-request, HTTP,
unnamed-control, duplicate-ID, or required-overflow failure is recorded.

Focused Python passes 230/230 in the locked environment. The ambient Python
run refused eight public-alpha cases because Markdown 3.10.3 does not match the
bound 3.10.2; the same unchanged tests pass in the repository environment.
The governed full gate remains non-green only at the already disclosed
unrelated stored example-transcript divergence. Nothing was recaptured or
blessed.

## Canonical isolation

Current source SHA-256:

- `src/web/browser/liturgy/day.html`:
  `bc5a98de6b718431f3b91e6a133bb847c2dcdf4d21fce6f45aae3ad4984de868`
- `src/web/browser/liturgy/index.html`:
  `f630f4a66f3f525144336f183b1485c698030c7531ff679375b3a7aa00150c65`

These are the planning-package hashes and prove neither canonical file was
promoted during compatibility closure.

## Regenerated future patch

`PROPOSED-CUTOVER.patch` is SHA-256
`cd11518ba7b20d198cbd16d08cc153d3f3053afeba8a230441c57312318a7566`
against exact execution baseline
`e62a226fc661100a2427a4193213c7dadcf24225`.
`git apply --check` passes without `--unidiff-zero`.

It proposes exactly 17 paths: two canonical HTML entry points, two release
records, seven Python owners, three Chromium harnesses, and three tracking
records. It contains no candidate, controller, shared-state, stylesheet,
renderer, adapter, source/data, navigation, or oracle hunk. It is not applied.

Prospective source hashes are
`9a119a6aa87e900d6fc4c3e236191fe8a036abc305236eb576c09f823c7b7972`
(Day),
`a6527316266365b79ff2ecdc193da3ab1034b1daa63408b869b192d2aeb85600`
(Propers), and
`5a4b4d4f8bfbcf8d5fd54249c0d8664be7215b4bee9e82ccc8a37f400e3cf31e`
(rights record).

## Stopping point

Answer only the seven questions in `REVIEW_REQUEST.md`. A pass may authorize a
later clean public-cutover execution phase. It does not itself authorize public
navigation changes, fallback cleanup, or a deployed cutover.

## Sealing record

- Canonical continuity and `PLAN-AND-CONTINUITY.md` are byte-identical at
  SHA-256
  `ee7efaac16feda4df834932de76ac79d977113f409ab44f86dec8835daeb6b76`.
- Canonical and packaged proposed patches are byte-identical at SHA-256
  `cd11518ba7b20d198cbd16d08cc153d3f3053afeba8a230441c57312318a7566`.
- The sealed directory contains 62 files including `MANIFEST.sha256`.
- `MANIFEST.sha256` is generated last and must verify 61 payload entries; it
  deliberately does not hash itself.
- The transport ZIP must contain exactly one top-level directory and pass
  `unzip -t`. Its externally calculated SHA-256 is recorded with the pushed
  archival commit and final handoff response because an archive cannot contain
  its own stable digest.
