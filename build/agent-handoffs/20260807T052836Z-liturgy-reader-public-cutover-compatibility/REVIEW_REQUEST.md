# Narrow independent review request

Please give an explicit PASS or FAIL with exact blockers for each question:

1. Does `why=1` preserve the existing public behavior without reintroducing
   the old visual wall?
2. Are multiple territorial branches faithfully exposed without inference or
   silent preference?
3. Do `cycle`, `alternative`, and `translation-witness` form a stable, tested
   public Propers contract?
4. Are retained candidates statically noindex and all shared user-visible
   strings route-neutral?
5. Is Details-based Day↔Propers/context navigation visually subordinate and
   usable?
6. Are canonical `day.html` and `index.html` still byte-unchanged?
7. Is the regenerated cutover patch complete, mechanically applicable, and
   free of unresolved public-contract choices?

If all seven pass, state separately whether public-cutover execution is safe to
authorize as a later phase. Public navigation changes and candidate/oracle
cleanup are not requested.
