# Runnable candidate

This is the compact private-preview subset from the reviewed correction
handoff with the Round 1 corrected Instrument CSS. It contains both
visual-reset pages, their local browser assets, selected missal structures,
2026 calendars, and held Douay-Rheims fragments used by the re-review states.

From the handoff root run `python3 -m http.server 8000`, then use the URLs in
`../START-HERE.md`. No external request is required. This candidate is noindex,
unlinked, non-public, and not a replacement for production Day or Propers.
