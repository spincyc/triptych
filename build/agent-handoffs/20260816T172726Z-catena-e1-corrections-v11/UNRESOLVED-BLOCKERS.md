# Unresolved blockers — open on purpose, none touched here

The independent reviews of this correction family enumerated their findings
in the durable `PROJECT-WORK.md` records on their own review branches, and
those review records remain the authoritative statement of each. This lane
executed the V10 review's **exact next action** — the inherited and accessor
claim closure, the truthful presentation of states that establish less, the
complete late vector, the packaged journal's ownership, and the package,
provenance and privacy corrections — and deliberately touched nothing else.

"Owner" below names the lane or role authorized to do the work. It is not a
person, and no individual is named anywhere in this package.

Everything below is open.

## Required production corrections

1. **Projection is not yet the sole semantic source beyond this
   inherited-claim boundary.** `chapterVoices()` and `absenceRows()` still
   rescan raw sources. The descriptor-based own-data reading closed the
   claim's contract and the request-composing members; it did not make the
   projection the sole semantic source for the route.
   Owner: the next authorized E1 correction lane.
2. **Orphan raw sources still manufacture offers and rows.** An orphan
   source manufactures a voice offer and an absence row that no projected
   record entitles.
   Owner: the next authorized E1 correction lane.
3. **A `source`-only fragment is invalid and still counts.** The blessing
   oracle still stands beside the source/locator fixture.
   Owner: the next authorized E1 correction lane; the producer contract it
   must enforce belongs to E0.
4. **Scalar and nested translator coercion.** The generator iterates a
   scalar `translators` value into letters, and the nested case is
   unhandled.
   Owner: the next authorized E1 correction lane.
5. **Malformed and padded absence findings.** Absence members still
   manufacture rows and suppress valid siblings, including the padded
   `" none-published "` false negative and the generator's coercion path.
   Owner: the next authorized E1 correction lane.
6. **A refusal is not a whole typed refusal member.** The kind-dependent
   `verse` is unchecked in five shapes.
   Owner: the next authorized E1 correction lane.
7. **Broader partial selection is neither closed nor order-independent.**
   Including the non-injective NUL-delimited ranking key. The prefix
   composition, presentation and claim closure this family of lanes closed
   are not the general case.
   Owner: the next authorized E1 correction lane.
8. **Unreadable roots still become Catena claims.** Trimmed data
   identities, incomplete `spineUnreadable`, empty-layer over-claims,
   break-at-missing verse, and the shared `verses` negative.
   Owner: the next authorized E1 correction lane.
9. **An unreadable `src/web/data/bibles.json` is locally suppressible, and
   its prose is unreviewed.** The V7-era reading that this was shared-shell
   behaviour was reversed by review: the forwarding seam is route-owned and
   must carry neutral copy.
   Owner: the next authorized E1 correction lane.
10. **Broader terminal proof remains partial outside this lane's vectors.**
    The full late-work sink vector for every corrected class is open. This
    lane completed the genuinely-late vector to all 36 guarded fields and
    the earlier lanes pinned the cold, present-valid and prewarmed vectors;
    the remaining corrected classes have no such vectors.
    Owner: the next authorized E1 correction lane.
11. **Several corrected oracles still bless the defects above.** This
    finding is recorded by the reviews as a class and **is not enumerated**
    — neither by the review records nor by this package. That is a gap in
    this record, stated rather than hidden, and it is not a claim that the
    class is small. Enumerating it is itself work that has not been done.
    Owner: the next authorized E1 correction lane, alongside each correction
    above.

## Architecture and payload

- **The CLI and web surfaces carry duplicated semantic models.**
  `scripts/_catena.py` and the browser model implement the same semantics
  twice. One neutral typed model callable by both surfaces is a separately
  authorized integration lane, per review.
  Owner: a separately authorized integration lane.
- **The uncapped model and combined route payload are disclosed, not
  governed.** This lane grew the unbudgeted model from 29,741 to 32,406
  gzipped whole and discloses it. Whether the model and the combined route
  payload need a governed ceiling is unanswered; `REVIEW_REQUEST.md`
  re-asks it.
  Owner: the budget owner.
- **The historical data-seam contradiction.** The historical
  `src/web/data/` Day-reader guard contradiction is unresolved.
  Owner: separately owned; not this lane and not the next E1 lane.

## Separately owned, and none of them this lane's

- **Four stale release bindings**, fail-closed and unsigned. None was
  re-signed here, and `make check-release-bindings` is red at both endpoints
  because of them.
- **The common browser gate's inherited failure population** — two `<main>`
  elements introduced by the release layout wrapping, and undersized
  navigation targets at the handset width. They are present on every route
  captured, including routes this correction does not touch.
- **B0 and the shared shell.**
- **Real-device and assistive-technology evidence.** None exists for this
  route; headless Chromium is not a device and is not an assistive
  technology.
- **Protected Liturgy.**
- **PDF prerequisites.**
- **The eventual reconciliation** of the cumulative implementation and
  review history at integration time.

## Package and evidence findings

The V10 review's package, provenance and privacy findings are answered **for
this package** by the corrected pipeline shipped under `logs/` and by its
products; `CLAIM-CLOSURE.md` and `PRIVACY-AUDIT.md` state what each
correction is. The earlier sealed packages themselves are named, untouched,
on their evidence branches, and remain exactly as their reviews left them.

**E1 is not integrated.** Current `origin/main` is
`e7f468e842727a817631d12f0854f8249556a8ff` and this lane is not merged with
it. No merge, re-signing, deployment or cutover is authorized by anything in
this package.
