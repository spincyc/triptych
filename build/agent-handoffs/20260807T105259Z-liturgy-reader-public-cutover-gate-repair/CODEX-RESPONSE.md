# Codex response

Gate repair ran in the disposable separate clone
`build/gate-repair-scratch.u1lspq/repo`, rooted at synchronized baseline
`e20b2f542ab51a2b4f0807e6394ca5ecb313699c`. It was never pushed or deployed.

The state test now requires both canonical pages to load exactly one shared
state contract, adapter, and route controller in dependency order. The Ordinary
tests retain and strengthen identity, notice, reading, action, surface,
accessibility, closed-dialog, and script/style-order checks. The metadata test
compares the safely serialized description across standard, Open Graph, and
Twitter metadata. The shared-shell test requires canonical readiness, settled
content, exactly one shell, and the correct entrance for each route. The Day
parity test's remaining legacy selector was also corrected after its first full
run exposed it.

The real repository retained the legacy canonical hashes throughout. No
cutover commit or Pages cutover deployment was attempted.
