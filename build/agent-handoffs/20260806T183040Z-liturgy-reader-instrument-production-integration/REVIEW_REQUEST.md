# Independent production-integration review request

Liturgical Instrument is already accepted as the production visual foundation.
Do not reopen its typography, reading measure, ritual grid, warning hierarchy,
masthead, Ordinary/Proper composition, or Date/Browse entrance model unless the
production integration exposes a concrete conflict with a frozen behavioral
seam.

Give an explicit **pass** or **fail** for each question:

1. Does the production Day reader match the accepted Instrument in Read and
   Missal at desktop, 1024, portrait tablet, mobile, deep scroll, Roman partial,
   and postconciliar partial-English states?
2. Does the production Propers reader belong to the same Instrument system in
   Read and Browse while correctly retaining the canonical production Browse
   selector rather than importing the out-of-scope prototype search control?
3. Are the rail/dock, 200% 2×2 reflow, four whole named labels, target sizes,
   forced colors, keyboard focus, reduced motion, and all open surfaces at
   accepted parity without overflow or obscured text?
4. Did integration preserve the accepted 636-pixel/about-75-character portrait
   measure, first-principal-text hierarchy, warning subordination, ritual cue
   continuity, and narrow mobile rhythm?
5. Did integration avoid regression to state/URL ownership, production
   adapters and renderers, Ordinary seating, fail-closed behavior, semantic
   location, modal focus/Escape/inert behavior, race ownership, source honesty,
   the visual oracle's unlinked/noindex isolation, and four one-step actions?
6. Is this production-integration candidate accepted, or what exact blockers
   remain? For each failure, name the route/hash, viewport, visible or behavioral
   symptom, and bounded correction.

If every question passes, state whether production integration is accepted.
Do not authorize public navigation or public cutover; each remains a separate
maintainer decision.
