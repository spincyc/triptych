# Start here

This is the deployed-parity production-integration review package for the
independently accepted **Liturgical Instrument** visual foundation. Public
navigation and public cutover remain unauthorized.

GitHub Pages run `31125898045` succeeded for exact intended production commit
`5444d89fc9b379a1babef5b2220323fe1508b2b3`. Direct Day, Propers, and accepted
oracle verification returned HTTP 200, and the deployed Instrument CSS and
candidate/oracle JavaScript byte-match source.

1. Read `REVIEW_REQUEST.md` for the exact integration dispositions requested.
2. Open `evidence/prototype-production-parity-contact-sheet.png`; each labeled
   pair places the accepted prototype first and the production candidate second.
3. Inspect all 23 production originals under
   `evidence/screenshots/parity/production/` at full size, with the accepted
   originals beside them under `evidence/screenshots/parity/prototype/`.
4. Read `MEASUREMENTS.md`, `PROTOTYPE-TO-PRODUCTION.md`, and
   `PRODUCTION-ISOLATION.md`.
5. Read `REVIEW-ROUND-1-ACCEPTANCE.md`, then `PLAN-AND-CONTINUITY.md` for the
   same verbatim acceptance in its durable context, Codex response,
   implementation decisions, checks, successful deployment parity, and exact
   stopping point.

The runnable candidate is in `candidate/`. Serve this directory with
`python3 -m http.server 8000`; exact example URLs are in its README.

This package asks for independent production-integration review only. It does
not ask for or authorize a public navigation change or public cutover.
