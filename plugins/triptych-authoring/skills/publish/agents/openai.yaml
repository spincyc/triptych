interface:
  display_name: "Publish"
  short_description: "Validate and publish Triptych documents"
  default_prompt: "Use $triptych-authoring:publish to validate, build, inspect, install, and catalog completed documents."
