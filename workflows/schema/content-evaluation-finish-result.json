{
  "name": "content-evaluation-finish-result",
  "description": "Structured result from the content evaluation fan-out of the proper-finish workflow, whose blocking findings name the stage that owns their repair -- brief for research/scope.md, which brief-revision corrects in place, or authoring for the canonical leaf -- and whose escalation findings name the artifact no stage in this run may write; there is no research owner, because this pipeline begins after research",
  "required_fields": [
    "stage",
    "iteration",
    "disposition",
    "findings"
  ],
  "valid_dispositions": [
    "PASS",
    "CHANGES_REQUIRED",
    "BLOCKED"
  ],
  "finding_fields": [
    "id",
    "severity",
    "location",
    "problem",
    "required_result"
  ],
  "blocking_finding_fields": [
    "repair_target"
  ],
  "finding_enums": {
    "severity": [
      "blocking",
      "accepted",
      "escalation",
      "advisory"
    ],
    "repair_target": [
      "brief",
      "authoring"
    ]
  },
  "optional_fields": [
    "summary",
    "lane",
    "lane_packet_hash",
    "observations"
  ],
  "escalation_finding_fields": [
    "escalated_to"
  ],
  "observation_fields": [
    "location",
    "note"
  ],
  "accepted_finding_fields": [
    "accepted_because"
  ],
  "optional_finding_fields": [
    "out_of_scope_reason"
  ]
}
