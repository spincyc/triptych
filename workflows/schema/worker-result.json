{
  "name": "worker-result",
  "description": "Structured result from a linear or revision worker stage",
  "required_fields": ["disposition", "summary", "packet_hash"],
  "valid_dispositions": ["PASS", "BLOCKED"],
  "finding_fields": [],
  "optional_fields": ["artifact_path", "notes", "block_reason"]
}
