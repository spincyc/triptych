{
  "name": "research-result",
  "description": "Structured result from a read-only research lane, and from the deterministic join of those lanes; every finding carries the retrieval receipts for the bytes its evidence was read from",
  "required_fields": [
    "stage",
    "iteration",
    "disposition",
    "summary",
    "findings"
  ],
  "valid_dispositions": [
    "PASS",
    "BLOCKED"
  ],
  "finding_fields": [
    "id",
    "claim",
    "evidence",
    "notes",
    "retrievals"
  ],
  "optional_fields": [
    "lane",
    "lane_packet_hash"
  ],
  "retrieval_fields": [
    "url",
    "sha256",
    "byte_size",
    "media_type",
    "path",
    "retrieved",
    "extent"
  ]
}
