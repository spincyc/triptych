# Web superscript repair: independent review

The code review and historical fidelity review are separate cold reviews.
The records in `code/` and `historical/` are verbatim reviewer outputs.
The coordinator rechecked every accepted subject hash and all 220 protected
source/support inputs before adopting the historical web changes.

The historical report refers to additional transient reproduction scripts,
command ledgers, diffs, regenerated Markdown and rendered HTML retained under
its named scratch directory; those are not durable archive contents. The
archived report, exact check summary, per-subject results and protected-input
manifest state the accepted scope and its limits.

No historical PDF review is claimed. Ordinary diff whitespace checking
reports three inherited semantic Markdown hard breaks; the narrow check
that permits end-of-line whitespace passes. New Sunday publication acceptance
is owned by each independent rite-specific workflow.
