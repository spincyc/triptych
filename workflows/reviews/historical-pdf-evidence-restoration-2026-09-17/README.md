# Restored historical PDF evidence

These are verbatim restoration records, not new content or visual reviews.
The coordinator independently rechecked all 20 installed PDF hashes and all
20 owning generation-metadata hashes before archiving these records.
No historical source, catalogue or release record changed for restoration.

The complete receipt file retains exact commands, exits and comparisons.
The report also names transient acquisition files and scratch-only scripts;
those are not contents of this archive. The original failing diagnostic log
contains an absolute local path and is deliberately not archived. Its exact
exit and 24-diagnostic disposition are retained in the public-safe report and
summary. The successful final output is preserved verbatim as `after.log`.

The restored PDFs remain ignored installed evidence. They do not enter this
archive or the new Sunday handoff and provide no renewed scholarly acceptance.
