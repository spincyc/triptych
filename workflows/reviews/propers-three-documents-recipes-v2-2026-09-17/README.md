# Proper-study v2 recipe review

`prepared/` contains verbatim independent cold-review records of the exact
five-file patch tested in isolation. Preparation passed; these original records are not evidence of live
application or a real v2 production. The later [live-application record](live-application/RECORD.md)
preserves the actual guarded application of the final six-file variant only
after both original v1 productions reached ACCEPTED.

The records name transient scripts, isolated fixtures and raw logs retained
under the review's scratch directory. They are not contents of this archive.
The archived commands, numeric exits, identities and negative-control outcomes
preserve the reviewed scope, including initial environment failures.

A separate integration supplement is required after actual guarded application,
current-toolchain tests and checks of both original runs' preserved identities.

`renderer-helper-addendum/` preserves a separate independent PASS for revised
patch `2ea50623c6d6090ce95309d2a7a27f231c8af0575e8a6dd1762a6dba54e02469`.
The sole delta is the newly shared renderer's fixture dependency. Five actual
command tests pass; removing that dependency reproduces the import failure.
The preparer's separate current verification records 106 owning tests and two
postconciliar fixtures; those are not additional reviewer executions. Neither
preparation changes a live workflow or accepts a Sunday production. This compact
archive omits the transient scripts, raw logs and isolated trees referenced by
the retained reports and manifests. Both original productions must reach their
actual terminal acceptance before guarded application and final integration
review.

`installation-addendum/` accepts the final six-file prepared patch
`55622214a655db4414b196319274d2ebbc528e268b44230ffd9447427bc85a13`.
It preserves the earlier five results and clarifies ordinary Make installation:
rebuilds must reproduce the previously accepted hashes; a mismatch is BLOCKED,
without a false PASS or invented automatic repair route. An actual worker
regression and sixth-subject refusal probe passed. The preparer separately
passed 106 owning tests and two postconciliar command fixtures. The final
prepared v2 digest is
`4e4bbd64b71b60bd9e04c9fa7592ecb52c85e05cad1349c41941fa4d31e4e4d2`;
all earlier patches and digests remain historical preparation evidence. This
archive retains exact reports, commands, identities and negative outcomes, not
the transient scripts, fixtures or raw logs they reference. The later live-application record preserves actual application and owning
checks. Independent final integration review remains separately required.

Archive whitespace verification: ordinary `git diff --cached --check` returned
2 solely for the required space prefix on a blank context line in the retained
installation delta patch. The exact patch is preserved; the scoped check with
`-c core.whitespace=-blank-at-eol` returned 0.


The [independent integration checkpoint](../propers-three-documents-integration-checkpoint-2026-09-17/REPORT.md)
subsequently passed the exact v2 application and first Sunday productions.
That historical approval does not satisfy the maintainer's later 10–12-page
concise/first-four-page revision; the overall task remains open.
