Verdict: **PASS for the prepared five-file proper-study v2 patch**, conditional on the already required sequencing: both real v1 productions must finish ACCEPTED before live application. No actionable defect was found in the reviewed patch. This verdict is neither production acceptance nor evidence that v2 has run a real publication.

The exact patch SHA-256 is `abcb8bea6d5c309802f13735f651a037e2d1d60f4a925f7bf0057ef6dff3ef59`. Review began on branch `feature/codex/propers/homily`, HEAD `8dfccbea84778e2694a47e4c1d4ea7addfcf6336`, with unrelated live work present. The patch was applied only to this review's isolated source copy. No live source, workflow, production state, provenance, Git index, or Git ref was changed by this review.

The five exact result identities are recorded in `result-hashes.json` and independently matched in `identity-evidence.json`. The reviewed pipeline differs structurally from the live v1 pipeline only in `version: 1` becoming `version: 2`. Its isolated v2 workflow digest is `e9bba4f684555ebfbba1bae6c30e51630802b7f6db7ba848fd9abb71385ce768`. The live v1 digest remained `1375f708d8670b2f4ccaf1869cfaa6fe67fe6946dc2b962c766bcdc499d3b47e`. The pipeline and two original recipe files also match their retained Git source at `af9b2d10a98a6aac2ce44cc84d6358ede8e630e8`.

Concrete review conclusions, with locations in the prepared result:

| Location | Conclusion and evidence |
| --- | --- |
| `workflows/fragments/proper-study/build-artifacts.md:18` | The dedicated `artifacts/build-artifacts-<iteration>/rasters` destination contains the renderer's replacement operation. `tools/pdf-review:1602` replaces the selected output root. The real regression renders three one-page PDF inputs, checks three page PNGs and three contact sheets, preserves exact sibling author-proof bytes, then removes an obsolete marker only within the raster child on repetition. |
| `workflows/fragments/proper-study/generate-web.md:5` | `--output build/web` agrees with `tools/web-edition:961`, which appends provider and document. Real Pandoc conversion and `snapshot-web` succeed for both `gpt` and `claude`; the receipt path and SHA-256 match the generated bytes, and no installed `web/` tree is created. |
| `workflows/OPERATOR.md:69` | The note accurately limits the change and retains the original v1 provenance. The frozen v1 Git source was independently checked. |
| `workflows/OPERATOR.md:75` | The replay description matches `scripts/_workflow.py:501` and `:1687`: an active run needs the current definition to match its recorded version and digest; a terminal run returns packet integrity with null deterministic/recompiled values. There is no historical-definition resolver. |
| `tools/tests/test_workflow_proper_study_recipes.py:65`, `:89`, `:125` | The two command tests invoke installed tools through copied real launchers. Their assertions detect the original defects. Only the third test substitutes a synthetic pipeline, fixture verdict, and repository commit; its scope is engine version/replay behavior. It verifies terminal inspection leaves the complete fixture run tree byte-identical. |
| `workflows/pipelines/proper-study.json:3` | The version increments without changing transitions, gates, source seals, family routing, or author/reviewer effort. The existing owning suite still exercises these contracts. |

The new test file uses the 1962 identity. To check the other physical tree independently, `probe-postconciliar.py` reuses its two real-command tests with the exact postconciliar identity `liturgy/roman-rite/postconciliar/roman-missal-third-edition-en-us-2011/propers/temporal/pc-s51-twenty-fifth-sunday-in-ordinary-time-year-a`. Both pass, including both providers' conversions, their snapshot receipts, and repeated rendering of all three PDF names. These are command fixtures under the correct paths, not theological content or real production reviews.

The current converter differs from the preparation's historical toolchain hash. Preparation used `010e7d412d24fa2497f54a84f4bb21b88afe8f804a0f10f563c573df86ae4de3`; this review tested `1ee4437ba79468709d75cad1c8f83dadcebfb7ae1ce148645aad20bb1b924fc2`. Inspection found only the independently reviewed Markdown superscript dialect repair documented in `workflows/reviews/propers-three-documents-web-superscripts-2026-09-17/code/review.md`; it does not change destination handling. The other ten recorded toolchain identities match preparation. `initial-evidence.json` preserves both identities and this difference rather than claiming the old helper hash is current.

Independent checks are recorded with exact commands and numeric exits in `checks.txt` and `commands.json`:

| Check | Exit | Observed result |
| --- | --- | --- |
| Base hashes and `git apply --check` through the prepared helper | 0 | All five subject expectations match; no files changed. |
| Exact patch application with `--whitespace=error` to the isolated copy | 0 | Exact five result hashes subsequently verified. |
| New recipe module | 0 | 3 tests passed, no skips. Real Pandoc, pdfLaTeX, pdftoppm, and ImageMagick executed. |
| Owning five-module suite after fixing review-environment Git discovery | 0 | 106 tests passed, no skips. |
| Same command tests with original v1 recipes temporarily restored in the isolated copy | 1 | Expected three assertion failures across two tests: duplicated `gpt/gpt`, duplicated `claude/claude`, and deleted sibling evidence. Prepared recipes were restored; result hashes were then verified. |
| Same real-command tests using the postconciliar identity | 0 | 2 tests passed, no skips. |
| Version-only pipeline comparison, frozen Git source, live freeze, and exact output hashes | 0 | All matched. |
| Guard refusal probes | 0 | Three inner application commands each returned 1 and changed no fixture bytes: an actual active-v1 state copy, drifted base bytes, and a preexisting new-test file. |

The first owning-suite attempt returned 1 with three errors because the review's Git discovery ceiling intentionally hid the parent repository; the other 103 tests passed. The final invocation removed that ceiling for the tests' read-only commit lookup and passed all 106. The initial toolchain-equality assertion also returned 1 because it detected the converter difference described above. These were review setup/dependency observations, not suppressed product failures.

The prepared application helper is a bounded guard, not an acceptance auditor. It checks patch and subject hashes, plus the two raw states' ACCEPTED/version/digest/manifest identity fields; it does not replay and independently establish all publication decisions. The coordinator's required real engine status and terminal packet-integrity captures therefore remain necessary. The cold-review status snapshot found `80a724fb8410dc3d` active at `install-publication` and `8f4e6454c021280a` active at `derive-homily`; both still recorded v1 and the frozen digest. This snapshot must not be reused as later acceptance evidence.

After both real runs are ACCEPTED, the coordinator still must preserve their final status/integrity evidence, apply these exact bytes under the guard, run the owning suite against the then-current helpers, verify both exact family identities, and verify old v1 status/integrity and provenance remain unchanged. A read-only integration supplement should bind this review to that applied diff and evidence before future v2 production. No new v2 production, live patch application, complete publication build, PDF visual acceptance, deployment, or historical deterministic recompilation was performed or claimed here.
