% Claude 1962 proper guide, Ninth Sunday after Pentecost, full-text edition:
% leaf-local typesetting primitives.
%
% This edition owns no research and no section text except its own appointed-
% text sheet. Every other section, and every shared presentation primitive,
% belongs to the study edition at the bare leaf id and is imported from it.
% This file therefore does three things and nothing else: it imports that
% leaf's format, it defines the one environment only a full-text edition
% needs, and it redefines the two passages whose wording differs between the
% two editions.
%
% Web-edition constraint: inherited from the study edition's format, which
% uses no landscape environment, no multicols, and no TikZ.

\input{liturgy/roman-rite/1962/propers/temporal/23-ninth-after-pentecost/format}

% The appointed Latin exactly as the controlling facsimile prints it. The
% frame marks received liturgical text that is not project-owned expression;
% the frame title carries the printed heading, the reference, and the marginal
% number so a reader can find it in the book.
\newenvironment{appointedlatin}[1]{%
  \begin{massbox}{#1}\itshape}{\end{massbox}}

% A rubric printed inside the formulary. It is not a prayer text and is not
% translated here; it is reproduced as printed and described.
\newcommand{\appointedrubric}[2]{\par\textbf{Rubric as printed:} \latin{#1}
\textperiodcentered{} #2\par}

\renewcommand{\latinpolicynote}{This is the full-text edition, so the complete
appointed Latin has already been printed above, in liturgical order and beside
the same English; the commentary quotes it again only where an argument turns
on the wording.}

\renewcommand{\editionnote}{\textbf{Which edition this is.} This is the
\textit{full-text edition}. It prints the complete appointed formulary --- the
Latin of the 1962 Vatican \latin{editio typica} with the English of the
registered public-domain witnesses --- as its own third section, and then
carries the study edition's map, dossiers, commentary and apparatus unchanged,
by importing those files rather than restating them. The \textit{study
edition}, at the same identity without the \texttt{-full-text} suffix, is
identical except that it does not print the appointed Latin in full. Both rest
on one body of research, carry the same collation findings, and share one set
of source records, which are kept with the study edition.}
