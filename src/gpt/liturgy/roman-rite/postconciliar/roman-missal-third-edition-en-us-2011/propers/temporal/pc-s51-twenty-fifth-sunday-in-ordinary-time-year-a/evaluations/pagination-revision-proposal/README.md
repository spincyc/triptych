# Pagination-revision proposal archive

**Unrendered and unreviewed proposal; no new acceptance.** This archive preserves
unfinished draft bytes for this owner only:
`gpt/liturgy/roman-rite/postconciliar/roman-missal-third-edition-en-us-2011/propers/temporal/pc-s51-twenty-fifth-sunday-in-ordinary-time-year-a`.
The historical accepted proper-study v1 run `8f4e6454c021280a` is unchanged.
The revised pagination requirement remains **OPEN**; this is not v3 production,
source verification, content approval, visual approval, or a publication input.

All 51 payloads are exact copies of the original files named
in `manifest.json`, with only a terminal `.draft` added to the archival filename.
Their original planning/status labels, source citations, relative scratch paths,
and any incomplete scaffold remain untouched. Statements in those files describe
the draft's original state, not a fresh check of current machinery or evidence.

The additional `adapter/chronology-inputs.toml.draft` belongs only to this postconciliar owner. It is an unadopted appointment-query candidate, not generated chronology or accepted source evidence.

`README.md`, `manifest.json` and `SHA256SUMS` are new archival wrappers, not
original draft material. The manifest maps every source root and original
relative filename to its archived filename, byte count, SHA-256 and draft status;
it also records excluded transient files. `SHA256SUMS` separately identifies
unchanged payloads and new wrappers; it excludes itself to avoid self-reference.
Original embedded manifests still name the original unsuffixed draft layout.

Run `sha256sum -c SHA256SUMS` from this directory before restoration. Restore
only to a new, family-specific scratch directory: copy each manifest-listed
payload to its recorded original relative filename, stripping only the final
`.draft`, and verify its bytes against the manifest. Keep the adapter candidate
in its separately identified source group where applicable. Do not overwrite a
canonical publication, alter historical packets/results, or treat restoration
as adoption. A later authorized v3 run must independently inspect and review
any material it actually uses; original scratch-relative imports may not resolve
in a restored location and are deliberately not rewritten here.

The inert suffix prevents recursive TeX render discovery without changing that
discovery rule. Nothing here is imported by the accepted entrypoints or declared
components. Existing source ownership, citation and third-party rights limits
remain in force; archival preservation supplies no additional rights clearance.
