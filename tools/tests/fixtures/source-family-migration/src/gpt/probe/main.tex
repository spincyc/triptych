% A synthetic leaf, never a publication. The bootstrap and refresh examples
% in tools/source-family-migration run with --root against a scratch copy of
% this directory (scripts/replay_examples.py makes it at
% build/example-migration). The inventory and classification review beside
% it are dated 2026-07-31 and never re-audited, so the --audited-on date
% those examples pass cannot come to predate them, as it did for the real
% records. The inventory pins this file's SHA-256: an edit here must
% regenerate both records in the same commit.
\section*{Probe}
A leaf with no sources.
